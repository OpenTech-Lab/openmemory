# Vendored copy of tree-sitter-javascript 0.21.4 from crates.io, patched via
# the workspace root Cargo.toml's [patch.crates-io]. The only change from
# upstream is the `cc` build-dependency requirement below (was "~1.0.90"),
# loosened to unblock adding the `russh` dependency: russh requires
# ring >=0.17.14, which itself requires cc ^1.2.8 — incompatible with
# tree-sitter-javascript's original tilde pin. cc's build API this crate
# uses (cc::Build::new()...compile()) is stable across the 1.x line, so this
# is a manifest-only change with no effect on the compiled output. See
# vendor/README.md for the full rationale and removal criteria.
#
# Original (unpatched) upstream content follows, for reference:
#
# THIS FILE IS AUTOMATICALLY GENERATED BY CARGO
#
# When uploading crates to the registry Cargo will automatically
# "normalize" Cargo.toml files for maximal compatibility
# with all versions of Cargo and also rewrite `path` dependencies
# to registry (e.g., crates.io) dependencies.
#
# If you are reading this file be aware that the original Cargo.toml
# will likely look very different (and much more reasonable).
# See Cargo.toml.orig for the original contents.

[package]
edition = "2021"
name = "tree-sitter-javascript"
version = "0.21.4"
authors = [
    "Max Brunsfeld <maxbrunsfeld@gmail.com>",
    "Douglas Creager <dcreager@dcreager.net>",
    "Amaan Qureshi <amaanq12@gmail.com>",
]
build = "bindings/rust/build.rs"
include = [
    "bindings/rust/*",
    "grammar.js",
    "queries/*",
    "src/*",
]
autoexamples = false
description = "JavaScript grammar for tree-sitter"
readme = "README.md"
keywords = [
    "incremental",
    "parsing",
    "tree-sitter",
    "javascript",
]
categories = [
    "parsing",
    "text-editors",
]
license = "MIT"
repository = "https://github.com/tree-sitter/tree-sitter-javascript"

[lib]
path = "bindings/rust/lib.rs"

[dependencies.tree-sitter]
version = ">=0.21.0"

[build-dependencies.cc]
version = "1"
