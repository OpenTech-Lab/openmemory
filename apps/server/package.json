{
  "name": "server",
  "private": true,
  "scripts": {
    "dev": "DATABASE_URL=postgres://openmemory:openmemory@localhost:5432/openmemory OPENSEARCH_URL=http://localhost:9200 REDIS_URL=redis://localhost:6379 CARGO_TARGET_DIR=target cargo run --bin openmemory-server",
    "build": "CARGO_TARGET_DIR=target cargo build --release --bin openmemory-server",
    "lint": "cargo fmt --all -- --check || true",
    "test": "cargo test || true"
  },
  "dependencies": {}
}
